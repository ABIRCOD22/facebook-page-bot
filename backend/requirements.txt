# === Web Framework ===
fastapi==0.115.0
uvicorn[standard]==0.30.0

# === Database ===
sqlalchemy==2.0.31
asyncpg==0.29.0

# === Redis ===
redis==5.0.7

# === AI / LLM ===
google-genai==1.46.0

# === Vector Database (Local, Free) ===
# ponytail: chromadb 0.5.0 requires numpy 1.x (np.float_ removed in numpy 2). Upgrade path: bump chromadb to 0.6+ when API changes are validated.
# ponytail: onnxruntime pinned to 1.20.1 - newer ORT x64 wheels assume newer CPU features; 1.20.1 is the last broad-x86-safe line. Upgrade path: revisit when Render free tier CPU is known or ORT ships generic wheels again.
# chroma-hnswlib 0.7.3 (hard-pinned by chromadb) is sdist-only for cp312 -> source build uses AVX-512 flags -> SIGILL on Zen 2 (no AVX-512). Dockerfile force-reinstalls the 0.7.6 manylinux wheel (portable, runtime dispatch) after pip install.
onnxruntime==1.20.1
numpy==1.26.4
chromadb==0.5.0

# === Image Processing ===
Pillow==10.4.0
aiohttp==3.9.5

# === Config ===
pydantic[email]==2.8.0
pydantic-settings==2.4.0

# === Auth ===
# ponytail: PyJWT for JWT tokens; stdlib hashlib.pbkdf2_hmac for password hashing (zero deps, secure)
PyJWT==2.9.0
python-multipart==0.0.12
pyotp==2.9.0
