{
  "$schema": "https://opencode.ai/config.json",
  "instructions": "Follow the ponytail ruleset in AGENTS.md: lazy senior dev mode. Before writing any code, climb the 7-rung ladder (YAGNI -> reuse -> stdlib -> platform -> installed dep -> one line -> minimum code). Never cut trust-boundary validation, data-loss-safe error handling, or security. Non-trivial logic leaves one runnable stdlib self-check."
}
